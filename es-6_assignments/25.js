// 25. Use keys(), values(), and entries() to iterate over the array indices, 
// values, and key-value pairs respectively.

let enames = ["rahul", "sonia", "priya", "modi"] 
for (let key of enames.keys()){
    console.log(enames)
}
